fastapi==0.139.2
uvicorn==0.51.0
groq==1.5.0
pydantic==2.13.4
networkx==3.6.1

# send_push_notification() in backend/logic/push_subscriptions.py remains a
# logging stub, not a real pywebpush call: no VAPID keys are provisioned and
# no live browser subscription exists yet (Thaariha's frontend half isn't
# built), so a real send would have nothing valid to send to. See that
# module's docstring for the full trade-off. pywebpush is intentionally NOT
# a dependency here — add it when a real send is wired up.
